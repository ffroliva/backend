spring:
  datasource:
    inicialization-mode: always
  main:
    lazy-initialization: true

  jpa:
    properties:
      hibernate:
        show_sql: true
        user_sql_commments: true
        format_sql: true

logging:
  level:
    org:
      hibernate:
        type: trace

